<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stu.otsea.dao.VideoPostDao">

    <resultMap type="com.stu.otsea.entity.po.VideoPost" id="VideoPostMap">
        <result property="videoId" column="video_id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="resourceId" column="resource_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="status" column="status" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="VideoPostMap">
        select
          video_id, user_id, title, resource_id, create_time, status
        from otsea.video_post
        where video_id = #{videoId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="VideoPostMap">
        select
          video_id, user_id, title, resource_id, create_time, status
        from otsea.video_post
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="VideoPostMap">
        select
        video_id, user_id, title, resource_id, create_time, status
        from otsea.video_post
        <where>
            <if test="videoId != null">
                and video_id = #{videoId}
            </if>
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="resourceId != null">
                and resource_id = #{resourceId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="videoId" useGeneratedKeys="true">
        insert into otsea.video_post(user_id, title, resource_id, create_time, status)
        values (#{userId}, #{title}, #{resourceId}, #{createTime}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="videoId" useGeneratedKeys="true">
        insert into otsea.video_post(user_id, title, resource_id, create_time, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.title}, #{entity.resourceId}, #{entity.createTime}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="videoId" useGeneratedKeys="true">
        insert into otsea.video_post(user_id, title, resource_id, create_time, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.title}, #{entity.resourceId}, #{entity.createTime}, #{entity.status})
        </foreach>
        on duplicate key update
        user_id = values(user_id) , title = values(title) , resource_id = values(resource_id) , create_time =
        values(create_time) , status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update otsea.video_post
        <set>
            <if test="userId != null and userId != ''">
                user_id = #{userId},
            </if>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="resourceId != null">
                resource_id = #{resourceId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where video_id = #{videoId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from otsea.video_post where video_id = #{videoId}
    </delete>

</mapper>