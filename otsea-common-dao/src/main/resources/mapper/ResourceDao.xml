<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stu.otsea.dao.ResourceDao">

    <resultMap type="com.stu.otsea.entity.po.Resource" id="ResourceMap">
        <result property="resourceId" column="resource_id" jdbcType="INTEGER"/>
        <result property="resourcePath" column="resource_path" jdbcType="VARCHAR"/>
        <result property="resourceType" column="resource_type" jdbcType="OTHER"/>
        <result property="infomation" column="infomation" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ResourceMap">
        select
          resource_id, resource_path, resource_type, infomation, create_time
        from otsea.resource
        where resource_id = #{resourceId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ResourceMap">
        select
          resource_id, resource_path, resource_type, infomation, create_time
        from otsea.resource
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ResourceMap">
        select
        resource_id, resource_path, resource_type, infomation, create_time
        from otsea.resource
        <where>
            <if test="resourceId != null">
                and resource_id = #{resourceId}
            </if>
            <if test="resourcePath != null and resourcePath != ''">
                and resource_path = #{resourcePath}
            </if>
            <if test="resourceType != null">
                and resource_type = #{resourceType}
            </if>
            <if test="infomation != null and infomation != ''">
                and infomation = #{infomation}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="resourceId" useGeneratedKeys="true">
        insert into otsea.resource(resource_path, resource_type, infomation, create_time)
        values (#{resourcePath}, #{resourceType}, #{infomation}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="resourceId" useGeneratedKeys="true">
        insert into otsea.resource(resource_path, resource_type, infomation, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.resourcePath}, #{entity.resourceType}, #{entity.infomation}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="resourceId" useGeneratedKeys="true">
        insert into otsea.resource(resource_path, resource_type, infomation, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.resourcePath}, #{entity.resourceType}, #{entity.infomation}, #{entity.createTime})
        </foreach>
        on duplicate key update
        resource_path = values(resource_path) , resource_type = values(resource_type) , infomation = values(infomation)
        , create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update otsea.resource
        <set>
            <if test="resourcePath != null and resourcePath != ''">
                resource_path = #{resourcePath},
            </if>
            <if test="resourceType != null">
                resource_type = #{resourceType},
            </if>
            <if test="infomation != null and infomation != ''">
                infomation = #{infomation},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where resource_id = #{resourceId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from otsea.resource where resource_id = #{resourceId}
    </delete>

</mapper>