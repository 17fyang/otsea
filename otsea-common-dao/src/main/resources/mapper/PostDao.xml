<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stu.otsea.dao.PostDao">

    <resultMap type="com.stu.otsea.entity.po.Post" id="PostMap">
        <result property="postId" column="post_id" jdbcType="INTEGER"/>
        <result property="authorId" column="author_id" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="postType" column="post_type" jdbcType="OTHER"/>
        <result property="resourceId" column="resource_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PostMap">
        select
          post_id, author_id, content, post_type, resource_id, create_time
        from otsea.post
        where post_id = #{postId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PostMap">
        select
          post_id, author_id, content, post_type, resource_id, create_time
        from otsea.post
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="PostMap">
        select
        post_id, author_id, content, post_type, resource_id, create_time
        from otsea.post
        <where>
            <if test="postId != null">
                and post_id = #{postId}
            </if>
            <if test="authorId != null and authorId != ''">
                and author_id = #{authorId}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="postType != null">
                and post_type = #{postType}
            </if>
            <if test="resourceId != null">
                and resource_id = #{resourceId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="postId" useGeneratedKeys="true">
        insert into otsea.post(author_id, content, post_type, resource_id, create_time)
        values (#{authorId}, #{content}, #{postType}, #{resourceId}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="postId" useGeneratedKeys="true">
        insert into otsea.post(author_id, content, post_type, resource_id, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.authorId}, #{entity.content}, #{entity.postType}, #{entity.resourceId}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="postId" useGeneratedKeys="true">
        insert into otsea.post(author_id, content, post_type, resource_id, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.authorId}, #{entity.content}, #{entity.postType}, #{entity.resourceId}, #{entity.createTime})
        </foreach>
        on duplicate key update
        author_id = values(author_id) , content = values(content) , post_type = values(post_type) , resource_id =
        values(resource_id) , create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update otsea.post
        <set>
            <if test="authorId != null and authorId != ''">
                author_id = #{authorId},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="postType != null">
                post_type = #{postType},
            </if>
            <if test="resourceId != null">
                resource_id = #{resourceId},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where post_id = #{postId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from otsea.post where post_id = #{postId}
    </delete>

</mapper>